# Sample GitOps repo

A minimal application manifest set used by the e2e test and convenient for
manual smoke tests. After `git init`-ing this directory and pushing to a
remote (or pointing `k8s-ui` at a local bare clone), register it as a
repository and create an Application with `path=app`.
