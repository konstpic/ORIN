# Umbrella chart for scaled ORIN (subcharts under charts/).
# Images are published to GHCR on tag push (see .github/workflows/ghcr.yaml).

global:
  image:
    tag: latest
    pullPolicy: IfNotPresent

  images:
    apiserver:
      repository: ghcr.io/konstpic/orin-apiserver
    controller:
      repository: ghcr.io/konstpic/orin-controller
    reposerver:
      repository: ghcr.io/konstpic/orin-reposerver

  adminToken: devtoken
  encryptionKey: "0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef"
  databaseUrl: ""

  postgres:
    enabled: true
    username: orin
    password: orin
    database: orin

  serviceAccount:
    name: ""

  rbac:
    clusterAdmin: true

  nodeSelector: {}
  tolerations: []

postgres:
  storage: 1Gi

apiserver:
  replicaCount: 2
  service:
    type: LoadBalancer
    port: 80
    nodePort: null
  resources: {}

controller:
  replicaCount: 2
  resources: {}
  appsCatalog:
    enabled: false
    repoUrl: ""
    path: orin/apps.yaml
    revision: HEAD
    interval: 5m

reposerver:
  replicaCount: 2
  autoscaling:
    enabled: true
    minReplicas: 2
    maxReplicas: 10
    targetCPUUtilization: 70
