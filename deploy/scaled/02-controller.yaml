# Controller deployment with leader election via PostgreSQL advisory lock.
# Multiple replicas can run — only one actively reconciles at a time.
# If the leader crashes, the lock is released and a standby takes over.

apiVersion: apps/v1
kind: Deployment
metadata:
  name: k8s-ui-controller
  namespace: k8s-ui
  labels:
    app.kubernetes.io/name: k8s-ui-controller
    app.kubernetes.io/component: controller
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: k8s-ui-controller
  template:
    metadata:
      labels:
        app.kubernetes.io/name: k8s-ui-controller
    spec:
      serviceAccountName: k8s-ui
      containers:
      - name: controller
        image: k8s-ui:dev
        args: ["controller"]
        env:
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: k8s-ui-secret
              key: database-url
        - name: ADMIN_TOKEN
          valueFrom:
            secretKeyRef:
              name: k8s-ui-secret
              key: admin-token
        - name: ENCRYPTION_KEY
          valueFrom:
            secretKeyRef:
              name: k8s-ui-secret
              key: encryption-key
        - name: IN_CLUSTER
          value: "true"
        - name: REPO_SERVER_ADDR
          value: "k8s-ui-reposerver:50051"
        - name: REPO_CACHE_DIR
          value: /tmp/k8s-ui-repos
        livenessProbe:
          exec:
            command: ["true"]
          initialDelaySeconds: 30
          periodSeconds: 30
        resources:
          requests:
            cpu: 200m
            memory: 256Mi
          limits:
            cpu: "1"
            memory: 1Gi
